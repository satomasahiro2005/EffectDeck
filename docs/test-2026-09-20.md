# 実機で見るもの（2026-09-20 のぶん）

版は `2026.09.20`。出ていない項目は「変えていない」ので見なくてよい。

## 0. まず繋ぐ

- [ ] ルートピッカーに EffectDeck が出る
  - 出ないなら: アプリを 1 回起動 → それでも出なければ iPhone を再起動
  - appex・entitlement・UTType は焼いたバンドルで確認済みなので、出ない理由は端末の登録状態
- [ ] **音を鳴らしたまま**選ぶ（止めている間に選ばない）
- [ ] 選んだあと音が来る

## 1. Spectrogram（配色）

- [ ] 帯に色が付く（黒 → 青 → シアン → 緑 → 黄 → 赤 → 白）
- [ ] **横線（周波数の格子）が帯の上に見える**。今回 z 順を直した所
- [ ] 1 秒の印（下端の短い縦線）が見える
- [ ] 図を指でなぞると横の破線が出て、音（プレビュートーン）が鳴る
- [ ] light mode でも読める（地は黒のまま。上流と同じ）
- [ ] Frequency Scale を Linear にして、**カードを畳んで開き直しても Linear のまま**
  - 前は Log に戻っていた

## 2. 畳んだ段

- [ ] GEQ / 15band PEQ / Modal Resonator / 5band Dynamic EQ / Multi Channel Panel /
      Earphone Cable Sim を「図だけ」の段にして、**押せない札が出ていない**
- [ ] Matrix と Oscillator は段が 2 つ（開く ↔ 畳む）になり、「図だけ」の段が無い
- [ ] バンドを選び直してから畳んで開き直すと、**選んだバンドのまま**
- [ ] Spectrum Analyzer の Bar display を入にして畳んで開き直すと、入のまま

## 3. Section

- [ ] **Section を消しても中のエフェクトが消えない**（畳んでいても）
  - いちばん大事。前は畳んだ Section をスワイプで払うと配下が全部消えていた
- [ ] 切ってある Section を消すと確認が出る。消したあと配下が鳴り出す
- [ ] 配下の ON/OFF は消す前のまま
- [ ] 組から下へ何度も出しても、**無名の Section が増え続けない**
- [ ] 組の唯一の配下を出したとき、何も起きない（振動も出ない）
- [ ] 畳んだ Section を動かすと配下が付いてくる（ここは前のまま）
- [ ] Effects 一覧の末尾に "Grouping" の節があり、そこに Section が居る
- [ ] 上の帯の最後にも "Grouping" のチップが出る
- [ ] 検索から Section を出したとき、チップも "Grouping" になっている

## 4. 並べ替えとスクロール

- [ ] **ゆっくりスクロールを始めても掴みが立たない**（スクロールが死なない）
- [ ] 長押しで掴むのは今までどおりできる
- [ ] 勢いよく送った直後でも掴める（惰性中は見ていないので）
- [ ] 掴んでいる最中に行が作り直されても、**スクロールが効かなくなったまま残らない**
- [ ] 端まで持っていっても自動では送らない（**自動スクロールはまだ入れていない**）

## 5. 打ち込み欄

以下の値の欄を触って、数字が打てるか。打ったあと外を触って確定するか。

- [ ] AU のパラメータ行（**1000 が `1e+03` にならない**）
- [ ] Room EQ
- [ ] Multi Channel Panel（Volume / Delay）
- [ ] Tube Simulator（Output Safety Trim）
- [ ] Earphone Cable Sim（共鳴の行）
- [ ] Note Spectrogram（Time Span）
- [ ] Spatial Mapper（行列の値）
- [ ] Phase Select EQ（Gain）
- [ ] Group Delay PEQ（Freq / Delay / Q）
- [ ] Crosstalk Cancellation（5 行）
- [ ] Group Delay EQ（Delay）
- [ ] **範囲の外を打っても飛ばない**（挟まれる）
- [ ] 単位付きの表示（`1.50 k`、`2.00 oct`）の欄を触ったとき、**下書きに数字だけが入る**

## 6. Phase Select EQ

- [ ] Band の札が「1 2 3 4 5」の 1 行になっている（前は折り返す 2 段の札）
- [ ] 選んでいる札が塗られる。切ってある札は薄い
- [ ] 下の一枚の頭に `BAND n` と電源トグルがある
- [ ] 電源トグルで入切できる。**入にしたとき図に出ない四角にならない**

## 7. 設定・問題報告

- [ ] About → Feedback → Report a problem → Add to it に「Copy details」がある
- [ ] 押すと診断が貼られる。**鳴らしている最中の数字**（溜まり・枯れ・詰め）が今の値になっている
- [ ] Audio → Details の「Copy details」も今までどおり効く
- [ ] Report a problem のリンクが EffectDeck のリポジトリを開く（旧 effetune-live ではない）

### 7b. 設定の並び（作り直した所）

- [ ] Audio の節が上から **Status → Processing → Power → Graphs → Plug-ins → Details**。
      見た目の設定（Sync Visuals・JSFX canvas）が Processing と Power のあいだに
      挟まっていない
- [ ] Sync Visuals to Audio の節に **Graphs** の見出しが出ている
- [ ] About の節は **About** と **Feedback** の 2 つ。「Contact」という見出しは無い
- [ ] About の footer の末尾に **Privacy policy** のリンクがあり、押すと開く
      （押せる行としては出ていない）
- [ ] **Acknowledgements** を押すと、礼の 4 件（EffeTune / ysfx / JSFX, WDL and EEL2 /
      PFFFT）が上に、条文の一覧が下に出る
- [ ] 条文の行を押すとその場で本文が開く（もう 1 枚 push されない）

## 8. 待機コスト（issue #5）

**前の数字が無いので、前後比較はできない。**壊れていないことだけ見る。

- [ ] 鳴っている最中に音が途切れない（30 分ほど流す）
- [ ] 無音のところで音が消えない（曲間・フェードの終わり）
  - 早抜けは入力が**厳密に 0** のときだけなので、弱音は素通しのまま
- [ ] 無音のあと音が戻るときに頭が欠けない
- [ ] 図を触ってプレビュートーンを鳴らすと、**鎖を通った音**になる（EQ の効きが聴こえる）
  - 素のサイン波が返るなら直っていない
- [ ] 一時停止 → 再生を往復しても、"no audio" が点滅し続けない
- [ ] 画面を消して戻したとき、図がすぐ動き出す
- [ ] コントロールセンターを引き下ろして戻しても、図が止まらない
  - 止めるのは `.background` だけにしてある。`.inactive` では止めない
- [ ] Bluetooth を繋いだときに 16000 / 32000 Hz へ落ちない
  - 入力の固定を実経路で判定する形に変えたので、ここが効いているか

## 9. 触っていないので壊れていないはずの所（怪しかったら見る）

- [ ] プリセットの保存・読み込み
- [ ] 共有リンクの書き出しと取り込み
- [ ] IR の取り込みと Reverb
- [ ] AU の取り込みと自前 UI
- [ ] 図（Spectrogram 以外）の見た目


---

# 2 回目に足したぶん（JSFX ブランチで焼いたもの）

入れたのは `codex/jsfx-host`。main の変更は全部取り込んであるので、上の項目も全部効く。

## 10. 並べ替え（直し直した）

- [ ] **掴んだまま、もう一方の指でスクロールできる**（1 本指では動かない）
- [ ] 指を足したとき、掴んでいる札が指の間へ寄らない
- [ ] 小さいカードを図付きの大きいカードの上へ動かすとき、**前より早く入れ替わる**
- [ ] 離したあと 1 本指のスクロールが戻る

## 11. FIR PEQ

- [ ] 図の印を掴んで動かせる（周波数とゲイン）
- [ ] **掴んでいる最中も狙いの曲線（破線）が指に追いてくる**
- [ ] 離した瞬間だけ設計が走る（掴んでいる間に音が途切れない）
- [ ] 出来上がりの曲線は、設定と一致しているときだけ出る
- [ ] 図に音が重なる（スペクトラム）
- [ ] 縦軸が ±20 になっていて、印が軸の端まで指に追いてくる
- [ ] 下の一枚の頭が `BAND n` ＋電源トグル（他の帯と同じ形）

## 12. AU

- [ ] カードの 1 行目がプラグイン名だけ（`Vendor: ` が付かない）
- [ ] 2 行目が `Audio Units · <作者>`
- [ ] **自前 UI を持たない AU を掴んでも、浮きカードが黒い板にならない**
- [ ] 既に鎖に載っていた AU も 1 行目から `Vendor: ` が消える

## 13. JSFX

- [ ] 値が指数表記にならない（1000 が `1e+03` にならない）
- [ ] 値を打ち込める
- [ ] カードの 2 行目に `JSFX · <作者>`（ソースに `author:` が在るとき）
- [ ] 見出しが `Trigger (trigger bits 1–10)`
- [ ] 鳴っていないとき trigger の札が押せない見た目になる
- [ ] つまみを回し続けても自動バイパスに落ちない
- [ ] 落ちたときはカードの頭に赤い 1 行＋ **Re-enable**。数字（`worst NN% of the block, N over`）も出る
- [ ] Re-enable を押すと戻る。**押した直後にまたすぐ落ちない**
- [ ] `.txt` を付けた JSFX も取り込める。JSFX でないテキストは弾かれる
- [ ] 弾かれたファイルが一覧に残らない
- [ ] 取り込んだ JSFX を一覧から左に払って消せる（同梱の見本は消えない）
- [ ] JSFX の canvas を触っている最中に、他のカードの数値入力が切れない

## 14. 問題報告

- [ ] About → Feedback → **Report a problem** → **Open an issue** を押すと、GitHub の issue 作成画面が
      診断とログの末尾入りで開く
- [ ] **Email** も同じく本文入りで開く
- [ ] 本文の末尾に「Attach log で添付してほしい」の 1 行がある
- [ ] Attach log で .txt が共有シートに出る

## 15. 経路（今回触った所）

- [ ] ルートピッカーを開いて閉じても、EffectDeck が一覧から消えない
      （`stopDeviceDiscovery` で `lostDevice` を呼ぶのをやめた所）
- [ ] 繋ぎ直しが今までどおりできる
