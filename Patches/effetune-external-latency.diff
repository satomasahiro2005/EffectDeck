diff --git a/dsp/core/abi.cpp b/dsp/core/abi.cpp
index 6ebbed1..e7cbb0b 100644
--- a/dsp/core/abi.cpp
+++ b/dsp/core/abi.cpp
@@ -416,10 +416,11 @@ et_status et_pipeline_configure(et_engine engine, const std::uint8_t *descriptor
 
 void et_pipeline_set_external_callback(et_engine engine,
                                        et_pipeline_external_process process,
+                                       et_pipeline_external_latency latency,
                                        void *context) {
   effetune::Engine *target = findEngine(engine);
   if (target != nullptr) {
-    target->setPipelineExternalCallback(process, context);
+    target->setPipelineExternalCallback(process, latency, context);
   }
 }
 
diff --git a/dsp/core/engine.cpp b/dsp/core/engine.cpp
index 31f97b6..c41094f 100644
--- a/dsp/core/engine.cpp
+++ b/dsp/core/engine.cpp
@@ -694,7 +694,9 @@ et_status Engine::configurePipeline(const std::uint8_t *descriptor,
   snapshot.node_count_ = node_count;
   snapshot.channel_count_ = max_channels_;
   for (std::uint32_t index = 0u; index < node_count; ++index) {
-    snapshot.latencies_[index] = parsed[index].external ? 0u : instanceLatency(parsed[index].instance);
+    snapshot.latencies_[index] = parsed[index].external
+                                     ? externalPipelineLatency(parsed[index].externalIndex)
+                                     : instanceLatency(parsed[index].instance);
   }
   PipelineLatencyUpdate update;
   const et_status status = preparePipelineLatencyUpdate(snapshot, update);
@@ -714,8 +716,10 @@ et_status Engine::configurePipeline(const std::uint8_t *descriptor,
 }
 
 void Engine::setPipelineExternalCallback(et_pipeline_external_process process,
+                                          et_pipeline_external_latency latency,
                                           void *context) noexcept {
   pipeline_external_process_ = process;
+  pipeline_external_latency_ = latency;
   pipeline_external_context_ = context;
 }
 
@@ -731,7 +735,7 @@ et_status Engine::capturePipelineLatencySnapshot(PipelineLatencySnapshot &snapsh
   snapshot.channel_count_ = max_channels_;
   for (std::uint32_t index = 0u; index < pipeline_count_; ++index) {
     snapshot.latencies_[index] = pipeline_[index].external
-                                     ? 0u
+                                     ? externalPipelineLatency(pipeline_[index].externalIndex)
                                      : instanceLatency(pipeline_[index].instance);
   }
   return ET_OK;
@@ -882,7 +886,7 @@ et_status Engine::applyPipelineLatencyUpdate(PipelineLatencyUpdate &update) noex
   }
   for (std::uint32_t index = 0u; index < pipeline_count_; ++index) {
     const std::uint32_t current_latency = pipeline_[index].external
-                                              ? 0u
+                                              ? externalPipelineLatency(pipeline_[index].externalIndex)
                                               : instanceLatency(pipeline_[index].instance);
     if (snapshot.latencies_[index] != current_latency) {
       return ET_ERR_STATE;
diff --git a/dsp/core/engine.h b/dsp/core/engine.h
index 75f05ab..bbed002 100644
--- a/dsp/core/engine.h
+++ b/dsp/core/engine.h
@@ -64,7 +64,9 @@ public:
 
   et_status configurePipeline(const std::uint8_t *descriptor,
                               std::uint32_t descriptor_bytes) noexcept;
-  void setPipelineExternalCallback(et_pipeline_external_process process, void *context) noexcept;
+  void setPipelineExternalCallback(et_pipeline_external_process process,
+                                   et_pipeline_external_latency latency,
+                                   void *context) noexcept;
   class PipelineLatencySnapshot;
   class PipelineLatencyUpdate;
   // Capture/apply run on the engine's owning thread between process calls. Preparation
@@ -102,6 +104,11 @@ public:
   [[nodiscard]] float *combined() noexcept { return arena_.combined(); }
   [[nodiscard]] float *bus(std::uint32_t index) noexcept { return arena_.bus(index); }
   [[nodiscard]] float *scratch(std::uint32_t index) noexcept { return arena_.scratch(index); }
+  [[nodiscard]] std::uint32_t externalPipelineLatency(std::uint8_t index) const noexcept {
+    return pipeline_external_latency_ == nullptr
+               ? 0u
+               : pipeline_external_latency_(pipeline_external_context_, index);
+  }
   [[nodiscard]] char *byteScratch() noexcept { return arena_.byteScratch(); }
   [[nodiscard]] std::uint8_t *telemetryStaging() noexcept { return arena_.telemetryStaging(); }
   [[nodiscard]] std::uint32_t telemetryCapacity() const noexcept {
@@ -196,6 +203,7 @@ private:
   bool pipeline_configured_ = false;
   bool pipeline_delay_history_dirty_ = false;
   et_pipeline_external_process pipeline_external_process_ = nullptr;
+  et_pipeline_external_latency pipeline_external_latency_ = nullptr;
   void *pipeline_external_context_ = nullptr;
 };
 
diff --git a/dsp/include/effetune/abi.h b/dsp/include/effetune/abi.h
index 19444e4..6d73a33 100644
--- a/dsp/include/effetune/abi.h
+++ b/dsp/include/effetune/abi.h
@@ -23,6 +23,8 @@ typedef et_status (*et_pipeline_external_process)(void *context, uint32_t extern
                                                    float *audio, uint32_t channel_count,
                                                    uint32_t frame_count, double time_seconds,
                                                    int8_t channel_spec);
+typedef uint32_t (*et_pipeline_external_latency)(void *context,
+                                                 uint32_t external_index);
 typedef struct et_runtime_event_state {
   uint32_t generation;
   uint32_t latched;
@@ -180,6 +182,7 @@ ET_EXPORT et_status et_pipeline_configure(et_engine engine, const uint8_t *descr
                                           uint32_t descriptor_bytes);
 ET_EXPORT void et_pipeline_set_external_callback(et_engine engine,
                                                   et_pipeline_external_process process,
+                                                  et_pipeline_external_latency latency,
                                                   void *context);
 ET_EXPORT uint32_t et_pipeline_latency(et_engine engine);
 ET_EXPORT et_status et_pipeline_process(et_engine engine, uint32_t channel_count,
